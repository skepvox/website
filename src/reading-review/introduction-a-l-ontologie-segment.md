---
title: 'Pipeline segment preview — Introdução à ontologia'
buffer: true
search: false
head:
  - ['meta', { name: 'robots', content: 'noindex' }]
---

# Pipeline segment preview — *Introdução à ontologia*

Internal, **non-public** reading-leaf preview (a buffer page: noindexed, unlisted, out of the sitemap,
local search, and LLM output). It joins the vendored pipeline metadata with **one** segment's prose body
(`pt` / `00-01-002-008`) from the book-pipeline export — by `(segmentPrefix, language)`, never by
`routePath` — to validate the future leaf-rendering model. It creates **no** public segment route; the
live work at `/louis-lavelle/introduction-a-l-ontologie` (12 fr chapter pages) was the legacy fr edition, removed in slice A5; this QA surface previews the committed pipeline-export data only.

<PipelineSegmentPreview />
